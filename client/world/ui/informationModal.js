// @format

'use strict';

g.world.informationModal = ($body) => {
  const $modal = $body.find('#js-information-modal');

  (function init() {
    // onClick();
  })();

  function onClick() {
    // gcc
  }
};
