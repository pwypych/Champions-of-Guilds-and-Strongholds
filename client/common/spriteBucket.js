// @format

'use strict';

// What does this module do
// It holds all sprites instantiated by world render, with by they corresponding entity id's
g.common.spriteBucket = () => {
  const spriteMap = {};
  return spriteMap;
};
